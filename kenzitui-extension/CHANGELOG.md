# Changelog

## 0.2.0
- Added background service worker bridge for privileged cookie access.
- Added host permission request flow per domain.
- Added dark popup with ENV preview textarea and explicit copy action.
- Added parser module (`env_parser.js`) and smoke tests.
- Added basic lint/test scripts via `package.json`.
- Added explicit CSP and extension icons.

## 0.1.0
- Initial popup UI with cookie-to-env conversion.
